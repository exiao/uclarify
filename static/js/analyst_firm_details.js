$(document).ready(function () {

    $(".rating-wrapper .description").tooltip({
	    placement: "right",
	    trigger: "hover"
	});

	$(".strength").tooltip({
	    placement: "right",
	    trigger: "hover"
	});

	$(".basic-info .rating").tooltip({
	    placement: "right",
	    trigger: "hover"
	});
});